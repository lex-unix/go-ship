version: "3.7"
name: "goship-test"
services:
    shared:
        build:
            context: docker/shared
        volumes:
            - shared:/shared
    vm1:
        # privileged: true
        build:
            context: docker/vm
        volumes:
            - shared:/shared
    vm2:
        # privileged: true
        build:
            context: docker/vm
        volumes:
            - shared:/shared
    app:
        build:
            context: docker/app
        ports:
            - 3000:3000
volumes:
    shared:
